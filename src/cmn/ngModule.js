define([
   'angular',
   'angular-ui-router'
], function(angular){
   'use strict';

   /**
    *
    * @class ngModule
    */
   var ngModule = angular.module('ngModule', ['ui.router']);

   return ngModule;
});